function greeter(person) {
    var a = 5;
    var b = 123;
    var c = a + b;
    return "Hello, " + person;
}

let user = "Jane User";

window.onload = () => {
    // document.body.innerHTML = greeter(user);
}
